package SolidPrinciples;

// breaking principle
public class DependencyInjection {
    public static void main(String[] args) {
        // it states that hight level module should not depend on low level module .
        // both should depend on the abstractions

        // second principle -> abstractions should not depend on the details . details should depend on
        // the abstractions


    }
}
